# Changes to Program

### 16 MAY 2025
- Added functionality for "browse" option under both input and output text-fields.  Now users can simply navigate to the input & output directories and hit "select" to have the path automatically added to the field.
- Added a "cancel" button for the user to click instead of hitting the close button